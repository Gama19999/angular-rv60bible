export const environment = {
  production: false,
  appName: 'biblia reina valera 1960',
  icons: 'media/icons/',
  version: '1.0.0',
  fetchLimit: innerWidth > 1300 ? 20 : 16,
  api: '/api/',
  platform: 'local'
};
